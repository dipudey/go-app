APP_NAME=
APP_ENV=local
APP_DEBUG=true
APP_HOST=localhost
APP_PORT=8000

# Database configuration
DB_CONNECTION=mysql # or postgres
DB_HOST=localhost
DB_PORT=8889
DB_DATABASE=goapp
DB_USERNAME=root
DB_PASSWORD=root

# JWT secret key for signing tokens
JWT_SECRET=your_jwt_secret_key_here

# Example: To generate JWT tokens, set JWT_SECRET to a strong random value.
# You can generate a random secret using:
#   openssl rand -base64 32

# After editing, copy this file to .env:
#   cp .env.example .env
# and update values as needed.
